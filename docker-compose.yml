version: '3'
services:
    redis:
        image: "redis"
        ports:
            - "6379:6379"
    web:
        # build: .
        image: "ppapi"
        ports:
            - "8000:8000"
        links:
            - redis

